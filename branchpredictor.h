#include <iostream>

using namespace std;

typedef struct _

class TwoBitPredictor {
	public:
		

		fase ();
		fase (int);
		bool init;
		bool regWrite;
		bool memRead;
		int inst;
		int rs, rt, rd;
		int imm;
		int addr;

};
